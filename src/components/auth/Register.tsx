import React from 'react';
import RegisterWithBusiness from './RegisterWithBusiness';

/**
 * Register Component - Now uses the enhanced RegisterWithBusiness for integrated registration
 */
const Register: React.FC = () => {
  return <RegisterWithBusiness />;
};

export default Register;
